{
  "name": "starfield-digipick-simulator",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist -c sistemagtarp.malaca.com.br"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1"
  },
  "dependencies": {
    "vite": "^8.1.5"
  }
}
